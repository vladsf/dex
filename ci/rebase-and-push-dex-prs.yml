---
platform: linux

image_resource:
  type: registry-image
  source: {repository: concourse/unit}

inputs:
  - name: concourse-dex

params:
  CONCOURSE_DEX_DEPLOY_KEY:

run:
  path: concourse-dex/ci/scripts/rebase-push-dex-prs
