---
platform: linux

image_resource:
  type: registry-image
  source: {repository: concourse/unit}

inputs:
  - name: dex
  - name: dex-github-release
  - name: concourse-dex-version

params:
  CONCOURSE_DEX_DEPLOY_KEY:

run:
  path: dex/ci/scripts/create-merged-tag
